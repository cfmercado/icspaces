import { Typography } from "@mui/material";

const FAQs = () => {
  return (
    <Typography variant="h6" mt={15}>
      This is where you put info on what the app is for, how to use it, etc.
    </Typography>
  );
};

export default FAQs;
