import { Typography } from "@mui/material";

const BookRoomPage_Admin = () => {
  return (
    <Typography mt={10}>
      Admin can book a reservation or cancel available reservation scheds
    </Typography>
  );
};

export default BookRoomPage_Admin;
